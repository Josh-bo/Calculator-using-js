function show(para){
    input.value += para
}
function equalTo(){
    inp.value = eval(input.value)
}
function del(){
    input.value = input.value.slice(0,-1)
}
function clean(){
     input.value = input.value.slice(0,-0)
     inp.value = inp.value.slice(0,-0)
}   